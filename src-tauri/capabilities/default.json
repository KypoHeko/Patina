{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Base permission set for the main window.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
